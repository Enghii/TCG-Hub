import { useLocalSearchParams } from 'expo-router'; import { SafeAreaView } from 'react-native-safe-area-context'; import { StyleSheet, Text, View } from 'react-native'; import { colors, spacing } from '@/src/theme';
const names: Record<string, string> = { market: 'Mercado', collection: 'Coleção', decks: 'Deck Builder', community: 'Comunidade', competitive: 'Competitivo' };
export default function ModulePlaceholder() { const { slug } = useLocalSearchParams<{ slug: string }>(); return <SafeAreaView edges={['bottom']} style={styles.safe}><View style={styles.container}><Text style={styles.icon}>◌</Text><Text style={styles.title}>{names[slug] ?? 'Módulo'}</Text><Text style={styles.text}>Este espaço já está preparado para a próxima etapa do TCG Hub.</Text></View></SafeAreaView>; }
const styles = StyleSheet.create({ safe: { flex: 1, backgroundColor: colors.background }, container: { flex: 1, alignItems: 'center', justifyContent: 'center', padding: spacing.xl }, icon: { fontSize: 64, color: colors.primary }, title: { color: colors.text, fontSize: 30, fontWeight: '900', marginTop: spacing.md }, text: { color: colors.muted, textAlign: 'center', lineHeight: 22, marginTop: spacing.sm } });
